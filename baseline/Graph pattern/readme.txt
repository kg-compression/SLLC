# the purpose of Graph  Pattern is the find the frequent patten of the KB
# we use the rule from rule mining step, then use this rule to compress KB
# for example, structure（s,p,o）->(s,p1,o1) could be replaced by a new simplified structure (s,p2,o2), this method will lose some semantic of 
# the origianl KB